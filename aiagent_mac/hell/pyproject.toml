# This project was generated with 0.3.24 using template: https://github.com/beeware/briefcase-template @ v0.3.24
[tool.briefcase]
project_name = "myagent"
bundle = "com.example"
version = "0.0.1"
url = "https://app.fairyao.site/hell"
license.file = "LICENSE"
author = "Lei Xia"
author_email = "xiahare@163.com"

[tool.briefcase.app.hell]
formal_name = "hell"
description = "My first ai agent app"
long_description = """More details about the app should go here.
"""
sources = [
    "src/hell",
]
test_sources = [
    "tests",
]

requires = [
    # Add your cross-platform app requirements here
]
test_requires = [
    # Add your cross-platform test requirements here
]

[tool.briefcase.app.hell.macOS]
universal_build = true
requires = [
    # Add your macOS-specific app requirements here
]

[tool.briefcase.app.hell.linux]
requires = [
    # Add your Linux-specific app requirements here
]

[tool.briefcase.app.hell.linux.system.debian]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.hell.linux.system.rhel]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.hell.linux.system.suse]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.hell.linux.system.arch]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.hell.linux.flatpak]
flatpak_runtime = "org.freedesktop.Platform"
flatpak_runtime_version = "24.08"
flatpak_sdk = "org.freedesktop.Sdk"

[tool.briefcase.app.hell.windows]
requires = [
    # Add your Windows-specific app requirements here
]

# Mobile deployments
[tool.briefcase.app.hell.iOS]
requires = [
    # Add your iOS-specific app requirements here
]

[tool.briefcase.app.hell.android]
requires = [
    # Add your Android-specific app requirements here
]

# Web deployments
[tool.briefcase.app.hell.web]
requires = [
    # Add your web-specific app requirements here
]

